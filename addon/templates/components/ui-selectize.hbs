<span class="ui-selectize{{if fingerFriendly ' finger-friendly'}}">
  <select
    id="select-{{elementId}}"
    class="{{type}}-input {{touchDevice}}{{if selected 'selected' 'not-selected'}}{{_mood}}{{_size}}"
    name="{{name}}"
    autocomplete={{autocomplete}}
    autofocus={{autofocus}}
    disabled={{disabled}}
  >
    {{yield}}
  </select>
</span>
